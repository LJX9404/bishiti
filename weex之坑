weex不支持z-index和display属性，遇到层级问题，只能把层级高的代码靠后写，需要隐藏的元素直接用v-if控制，这样就不会导致隐藏的弹框影响层级；
在项目中的解决办法是把隐藏的弹框宽高置为1而不是0，也可以解决底部菜单菜谱切换异常的问题，只是此时隐藏的弹框依旧占位
weex不支持同一方向滚轮嵌套的问题：把weex-amui的picker-view样式进行修改，让picker-view的高度达到理想数值让容器盒子的高度不超过可视区
